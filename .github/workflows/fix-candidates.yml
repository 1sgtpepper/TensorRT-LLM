# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Check guided decoder review fixes

on:
  push:
    branches: [validation/guided-review-ci]
    paths: [.github/workflows/fix-candidates.yml]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-check:
    strategy:
      fail-fast: false
      matrix:
        include:
          - revision: 34836d506de2223448b5227d61f4d422ca74199a
            test: test_guided_recompute.py
          - revision: 7c30b1283d2e90e0a0aeee2cf88022143f697ddc
            test: test_guided_tree.py
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ matrix.revision }}
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: guided-recompute-hooks-py312-${{ hashFiles('.pre-commit-config.yaml') }}
      - name: Run repository release checks on changed files
        run: |
          test "$(git rev-parse HEAD)" = ${{ matrix.revision }}
          git fetch --depth=1 origin 17803f6c0cbe8d9fed3ccf46a295b867e3493d3e
          git diff --name-only 17803f6c0cbe8d9fed3ccf46a295b867e3493d3e HEAD > /tmp/changed-files.txt
          python -u scripts/release_check.py --files-from /tmp/changed-files.txt
          git diff --exit-code
      - name: Verify the GPU regression is selected by CI
        run: |
          python scripts/test_to_stage_mapping.py --tests unittest/_torch/${{ matrix.test }} > /tmp/tree-stages.txt
          cat /tmp/tree-stages.txt
          test -s /tmp/tree-stages.txt
          grep -q A10-PyTorch /tmp/tree-stages.txt
      - name: Show hook changes
        if: failure()
        run: git diff
